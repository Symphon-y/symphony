-- Target: ~/.config/hypr/monitors.lua
--
-- One monitor, auto-detected: true on the VM today, and a sane default until a real
-- monitor layout is worth hand-tuning (Phase 10, physical hardware).

hl.monitor({
  output = "",
  mode = "preferred",
  position = "auto",
  scale = "auto",
})
